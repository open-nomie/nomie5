import ExtractTrackers from "./extract-trackers";

import Log from "../../utilities/log/log";

const log = new Log("🔔🔔🔔🔔 TRACKER PROVIDER TESTS 🔔🔔🔔");

export default class ExtractTrackersTest {
  constructor() {}
  run() {
    try {
    } catch (e) {
      log.error(e);
    }
  }
}
